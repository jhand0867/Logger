namespace Logger
{
    class StateAll : stateRec
    {
        //public void ValidateAllState()
        //{

        //    Dictionary<string, stateRec> resultData = new Dictionary<string, stateRec>();
        //    Dictionary<string, stateRec> resultDataNext = new Dictionary<string, stateRec>();

        //    resultData = ValidateStateNumber("000");

        //    foreach (KeyValuePair<string, stateRec> rec in resultData)
        //    {
        //        string[] stateNum = bringStates(rec);
        //        //stateNum[0] = rec.Value.sta2;
        //        //stateNum[1] = rec.Value.sta7;
        //        string[] chain = "000";

        //        foreach (int i = 0; i < stateNum.Length; i++)
        //        {
        //            resultDataNext = ValidateStateNumber(stateNum[i]);
        //            chain = chain + stateNum[i];

        //        }

        //    }



        //}
    }
}
